{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*|bun/*) ;; *) echo \"Use pnpm or bun instead\" >&2; exit 1 ;; esac'",
    "dev": "node scripts/run-portfolio-vite.mjs --host 0.0.0.0",
    "build": "node scripts/run-portfolio-vite.mjs build",
    "build:dev": "node scripts/run-portfolio-vite.mjs build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.4",
    "typescript": "~5.9.3"
  }
}
